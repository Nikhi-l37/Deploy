# Auto-generated by Deploy

# HTTP -> redirect to HTTPS
server {
    listen 80;
    server_name jeevanhackthon.deploy.local;
    return 301 https://$host$request_uri;
}

# HTTPS server block
server {
    listen 443 ssl;
    server_name jeevanhackthon.deploy.local;

    ssl_certificate /etc/letsencrypt/live/deployat.me-0001/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/deployat.me-0001/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        # Wake-on-Demand: Ask FastAPI if the container is sleeping
        auth_request /wake;
        
        proxy_pass http://127.0.0.1:8003;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Timeouts for wake-on-demand (container may need time to start)
        proxy_connect_timeout 10s;
        proxy_read_timeout 30s;
        proxy_send_timeout 10s;
        
        # WebSocket support (important for real-time Node.js apps)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # If the container is still starting, show a friendly error instead of 502
        error_page 502 504 @wake_fallback;
    }
    
    location @wake_fallback {
        return 302 /wake-page/1ea20d22-ef42-42c1-8a66-7ea9f0fb8220;
    }
    
    location = /wake {
        internal;
        proxy_pass http://127.0.0.1:8888/gateway/1ea20d22-ef42-42c1-8a66-7ea9f0fb8220;
        proxy_pass_request_body off;
        proxy_set_header Content-Length "";
        proxy_set_header X-Original-URI $request_uri;
    }
}

# HTTP -> redirect to HTTPS
server {
    listen 80;
    server_name jeevanhackthon-adb1.deploy.local;
    return 301 https://$host$request_uri;
}

# HTTPS server block
server {
    listen 443 ssl;
    server_name jeevanhackthon-adb1.deploy.local;

    ssl_certificate /etc/letsencrypt/live/deployat.me-0001/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/deployat.me-0001/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        # Wake-on-Demand: Ask FastAPI if the container is sleeping
        auth_request /wake;
        
        proxy_pass http://127.0.0.1:8005;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Timeouts for wake-on-demand (container may need time to start)
        proxy_connect_timeout 10s;
        proxy_read_timeout 30s;
        proxy_send_timeout 10s;
        
        # WebSocket support (important for real-time Node.js apps)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # If the container is still starting, show a friendly error instead of 502
        error_page 502 504 @wake_fallback;
    }
    
    location @wake_fallback {
        return 302 /wake-page/64151161-6712-4633-aba2-195c0a0788a5;
    }
    
    location = /wake {
        internal;
        proxy_pass http://127.0.0.1:8888/gateway/64151161-6712-4633-aba2-195c0a0788a5;
        proxy_pass_request_body off;
        proxy_set_header Content-Length "";
        proxy_set_header X-Original-URI $request_uri;
    }
}

# HTTP -> redirect to HTTPS
server {
    listen 80;
    server_name 06e5c202.deploy.local;
    return 301 https://$host$request_uri;
}

# HTTPS server block
server {
    listen 443 ssl;
    server_name 06e5c202.deploy.local;

    ssl_certificate /etc/letsencrypt/live/deployat.me-0001/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/deployat.me-0001/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        # Wake-on-Demand: Ask FastAPI if the container is sleeping
        auth_request /wake;
        
        proxy_pass http://127.0.0.1:8004;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Timeouts for wake-on-demand (container may need time to start)
        proxy_connect_timeout 10s;
        proxy_read_timeout 30s;
        proxy_send_timeout 10s;
        
        # WebSocket support (important for real-time Node.js apps)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # If the container is still starting, show a friendly error instead of 502
        error_page 502 504 @wake_fallback;
    }
    
    location @wake_fallback {
        return 302 /wake-page/06e5c202-3850-4fba-896e-44c61fc10ee6;
    }
    
    location = /wake {
        internal;
        proxy_pass http://127.0.0.1:8888/gateway/06e5c202-3850-4fba-896e-44c61fc10ee6;
        proxy_pass_request_body off;
        proxy_set_header Content-Length "";
        proxy_set_header X-Original-URI $request_uri;
    }
}

# HTTP -> redirect to HTTPS
server {
    listen 80;
    server_name attendance-managment-67d4.deploy.local;
    return 301 https://$host$request_uri;
}

# HTTPS server block
server {
    listen 443 ssl;
    server_name attendance-managment-67d4.deploy.local;

    ssl_certificate /etc/letsencrypt/live/deployat.me-0001/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/deployat.me-0001/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        # Wake-on-Demand: Ask FastAPI if the container is sleeping
        auth_request /wake;
        
        proxy_pass http://127.0.0.1:8002;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Timeouts for wake-on-demand (container may need time to start)
        proxy_connect_timeout 10s;
        proxy_read_timeout 30s;
        proxy_send_timeout 10s;
        
        # WebSocket support (important for real-time Node.js apps)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # If the container is still starting, show a friendly error instead of 502
        error_page 502 504 @wake_fallback;
    }
    
    location @wake_fallback {
        return 302 /wake-page/e0a93ece-0e50-446d-9ec5-219616b8d3df;
    }
    
    location = /wake {
        internal;
        proxy_pass http://127.0.0.1:8888/gateway/e0a93ece-0e50-446d-9ec5-219616b8d3df;
        proxy_pass_request_body off;
        proxy_set_header Content-Length "";
        proxy_set_header X-Original-URI $request_uri;
    }
}

# HTTP -> redirect to HTTPS
server {
    listen 80;
    server_name attendance-managment.deploy.local;
    return 301 https://$host$request_uri;
}

# HTTPS server block
server {
    listen 443 ssl;
    server_name attendance-managment.deploy.local;

    ssl_certificate /etc/letsencrypt/live/deployat.me-0001/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/deployat.me-0001/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        # Wake-on-Demand: Ask FastAPI if the container is sleeping
        auth_request /wake;
        
        proxy_pass http://127.0.0.1:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Timeouts for wake-on-demand (container may need time to start)
        proxy_connect_timeout 10s;
        proxy_read_timeout 30s;
        proxy_send_timeout 10s;
        
        # WebSocket support (important for real-time Node.js apps)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # If the container is still starting, show a friendly error instead of 502
        error_page 502 504 @wake_fallback;
    }
    
    location @wake_fallback {
        return 302 /wake-page/70bfd8fc-92e8-4469-99a9-9268f0378091;
    }
    
    location = /wake {
        internal;
        proxy_pass http://127.0.0.1:8888/gateway/70bfd8fc-92e8-4469-99a9-9268f0378091;
        proxy_pass_request_body off;
        proxy_set_header Content-Length "";
        proxy_set_header X-Original-URI $request_uri;
    }
}